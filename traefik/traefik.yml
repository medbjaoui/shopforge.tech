log:
  level: INFO

entryPoints:
  web:
    address: ":80"   # interne uniquement — cloudflared pointe ici

providers:
  docker:
    exposedByDefault: false
    network: shopforge_network
    endpoint: "unix:///var/run/docker.sock"
